# Launch — Fleet Finance Signals

Start a fresh Codex thread in the repository and make it the **Manager**. Paste/run:

```text
/goal Read long-horizon/START.md and execute offer 02-fleet-finance as the Manager. Build the full P0 MVP from offers/02-fleet-finance/EXECUTION_PACK.md. Do not write product code yourself. Initialize the durable checklist, freeze acceptance, spawn one persistent Implementer, and run every phase through the Implementer in /goal mode using "completely, extremely well". Independently gate and review every phase before advancing. Continue until the final readiness gate passes or a legitimate external blocker terminal state is recorded.
```

## Expected terminal proof

The Manager must provide:

- final `run_state.json`;
- completed `checklist.json`;
- `acceptance_results.json`;
- reviewer PASS evidence per phase;
- final integration commit SHA;
- `.loop/progress.html`;
- unresolved external blockers, if any;
- exact command proving final readiness.
